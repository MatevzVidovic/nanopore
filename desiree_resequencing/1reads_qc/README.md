

### !!!Warning!!!

5old_reads.sh has not been locally tested.
Also, it uses an absolute path to Tim's profile:
oldreads_filepath="/scratch/timg/desiree_seq/input/ont/desiree_ont_raw.fastq"

We aren't sure if we can symlink to it or not - perhaps we could add a symlink step to add it to inputs.